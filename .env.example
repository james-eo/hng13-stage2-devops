# Blue/Green deployment configuration
# These variables will be set by the CI/grader system

# Container images for blue and green deployments  
BLUE_IMAGE=your-registry/nodejs-app:blue-tag
GREEN_IMAGE=your-registry/nodejs-app:green-tag

# Active pool determines which upstream is primary (blue|green)
ACTIVE_POOL=blue

# Release identifiers passed to containers and returned in X-Release-Id header
RELEASE_ID_BLUE=blue-release-v1.0.0
RELEASE_ID_GREEN=green-release-v1.0.0

# Application port inside containers (optional, defaults to 8080)
PORT=8080

# Stage 3: Observability & Alerts Configuration
# Replace with your actual Slack webhook URL
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/SLACK/WEBHOOK

# Error rate threshold (percentage) - alerts when exceeded
ERROR_RATE_THRESHOLD=2

# Number of requests in sliding window for error rate calculation
WINDOW_SIZE=200

# Cooldown period between alerts of the same type (seconds)
ALERT_COOLDOWN_SEC=300